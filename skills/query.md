# Query 工作流：查询知识库

> 本文档供 LLM 执行 `/query` 命令时参考。每次查询时按以下流程执行。

---

## 触发方式

```
/query 我们的用户在支付流程中最大的痛点是什么？
/query 竞品在导航设计上做了哪些不同的选择？
/query 新功能 X 的用户接受度如何？满意度如何？
```

---

> ⚠️ **重要约束**：查询时，**必须先通过 qmd 搜索来发现相关文件，不得凭借对 index.md 的已知内容直接选择文件**。qmd 能发现关键词不明显但语义相关的页面（如 `social-play-motivation` 对"山头服用户为什么流失"语义相关但无直接关键词），而手工选择文件存在遗漏风险。

## 完整流程

### 步骤 1：通过 qmd 搜索发现相关文件（必须执行，不可跳过）

> 🚫 **无论 wiki 规模大小（即使只有 5 个页面），都必须使用 qmd 搜索。**
> 原因：qmd 能发现语义相关但关键词不匹配的页面，手动选择文件存在遗漏风险。
> 即使用户自认为"知道答案在哪个文件"，也必须先搜索再读取。

> ❌ **禁止**：凭借对 index.md、log.md 或之前对话的已知内容，直接选择文件读取。
> ✅ **必须**：将用户查询交给 qmd，由搜索引擎客观排序后，再决定读哪些文件。

> 💡 `-c` 是 `--collection` 的简写。`qmd update` 和 `qmd embed` 不支持指定集合，会处理所有已注册集合。

**首先确认向量索引状态**：

```bash
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js status
```

查看 `status` 输出中 `Vectors` 一行，按结果执行对应流程：

---

#### 情况 A：`Vectors: 0 embedded`（向量未就绪）→ 自动修复

> 🚫 **禁止跳过！不要降级到纯 BM25 搜索。** 必须先建好向量再查询。

**立即执行以下两步**：

```bash
# 1. 确保 BM25 索引最新
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js update

# 2. 自动生成向量嵌入（首次约 1-2 分钟，需下载 ~1.3GB 模型）
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js embed
```

> ⚠️ `qmd embed` 运行时**没有进度条输出**，这是正常行为，耐心等待即可。
> 首次运行会自动下载 AI 模型，中国大陆用户需确保已配置 `HF_ENDPOINT=https://hf-mirror.com`。

完成后重新检查状态：
```bash
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js status
```

确认 `Vectors: N embedded`（N > 0）后，进入混合搜索。

---

#### 情况 B：`Vectors: N embedded`（N > 0）→ 执行混合搜索

**使用三阶段混合搜索**（BM25 + 向量语义 + LLM 重排序）：

```bash
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js query "{{用户的查询关键词}}" -c {{WIKI_NAME}} -n 10
```

> ⚠️ **注意**：`qmd query` 在向量索引未就绪时会**静默返回空**（无任何输出或报错）。如果运行后无输出，请重新检查 `qmd status`。

**如果 `qmd query` 结果不够丰富**，补充一次 BM25 搜索捕获关键词精确匹配：

```bash
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js search "{{关键词1}} {{关键词2}}" -c {{WIKI_NAME}} -n 10
```

---

#### 情况 C：`qmd embed` 失败（网络/内存问题）→ 降级搜索

仅在 embed 确实无法完成时（如网络不通、内存不足），才允许降级：

```bash
node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js search "{{关键词1}} {{关键词2}}" -c {{WIKI_NAME}} -n 10
```

同时告知用户：
```
⚠️ 向量索引建立失败（原因：[具体错误]），本次降级为 BM25 关键词搜索。
搜索质量受限：语义相关但关键词不匹配的内容可能遗漏。
建议排查后运行：node <SKILL_PATH>/scripts/qmd/dist/cli/qmd.js embed
```

---

**搜索结果即为候选文件列表**，按相关性得分排序。取 top 5~10 进入下一步。

---

### 步骤 2：读取 index.md（辅助，仅用于补全盲区）

读取搜索结果后，**可选**地扫描 `index.md`，检查是否有 qmd 搜索遗漏的明显相关页面：

```bash
cat "{{WIKI_PATH}}/wiki/index.md"
```

> ⚠️ 注意：index.md **仅作为 qmd 搜索的补充**，绝不能替代 qmd 搜索。无论 wiki 规模大小，都必须先完成步骤 1 的 qmd 搜索，再考虑是否需要 index.md 补盲。
> 如果 qmd 已返回 10 个相关结果，可跳过此步骤。

---

### 步骤 3：读取 qmd 返回的候选文件

根据步骤 1 的搜索结果，读取最相关的页面：

```bash
# 读取单个页面
qmd get wiki/concepts/支付流程痛点.md

# 批量读取
qmd multi-get "wiki/concepts/支付流程痛点.md,wiki/entities/用户-A.md,wiki/synthesis/支付优化建议.md"

# 读取页面特定段落（行号）
qmd get wiki/concepts/支付流程痛点.md:50 -l 100
```

---

### 步骤 4：生成综合答案

基于读取的页面内容，生成高质量的综合答案。

**答案的核心要求**：
1. **带引用**：每个论断都要注明来自哪个页面（哪份原始资料）
2. **综合多来源**：不偏向单一来源，综合所有相关证据
3. **标注矛盾**：如果不同来源有冲突，明确指出
4. **说明置信度**：区分"多来源强支撑"和"单一来源初步发现"

**答案格式示例（Markdown 页面）**：

```markdown
## 支付流程用户痛点分析

基于 3 份资料（2 次用户访谈 + 1 份竞品分析），支付流程痛点主要集中在两个维度：

### 1. 步骤数量过多（置信度：高，2/2 访谈提及）

用户普遍反映支付流程需要"太多步骤"：
- 用户 A 明确表示需要 3 步确认才能完成支付（[[sources/用户访谈-2024-03]]）
- 用户 C 同样提到"流程太繁琐"（[[sources/用户访谈-2024-01]]）

> 💡 与竞品对比：主要竞品已将支付步骤压缩至 2 步（[[sources/竞品分析-2024-02]]）

### 2. 加载速度慢（置信度：高，2/2 访谈提及）

加载性能问题在两次访谈中均被独立提及：
- "有时候点了没反应" —— 用户 A（[[sources/用户访谈-2024-03]]）
- 用户 C 描述为"页面卡顿"（[[sources/用户访谈-2024-01]]）

> ⚠️ **矛盾**：竞品分析显示，快捷支付功能在竞品中因安全顾虑接受度低
> （[[sources/竞品分析-2024-02]]），但我们的用户对此未表达明显担忧。
> 安全性认知差异的原因尚待研究。

### 总结

支付优化需要"步骤简化"与"性能提升"双管齐下，两者相互影响用户感知。
建议参考：[[synthesis/支付优化建议]]
```

---

### 步骤 5：根据用户需求调整答案形式

根据用户的实际需求，可以将答案以不同形式呈现：

#### 形式 A：Markdown 摘要页面（默认）
见上方示例。适合深度分析和需要归档的场景。

#### 形式 B：对比表格

```markdown
| 维度 | 我们的产品 | 竞品 A | 竞品 B |
|------|-----------|--------|--------|
| 支付步骤数 | 3 步 | 2 步 | 1 步 |
| 加载时间 | 3-5s | <2s | <1s |
| 用户满意度 | 中（访谈反馈） | 高（[[sources/竞品分析-2024-02]]） | 未知 |
```

#### 形式 C：Marp 幻灯片格式

```markdown
---
marp: true
theme: default
---

# 支付流程痛点分析

**来源**：2 次用户访谈 + 1 份竞品分析

---

## 核心发现

1. **步骤过多**：平均 3 步，竞品已做到 2 步
2. **加载慢**：2/2 用户自发提及
3. **机会**：快捷支付方向正确，需解决性能问题

---
```

#### 形式 D：行动项清单

```markdown
## 基于研究洞察的行动建议

### 高优先级
- [ ] 减少支付步骤至 2 步（有明确用户诉求）
- [ ] 优化支付页面加载性能（目标 <2s）

### 中优先级
- [ ] 研究用户对安全性的实际担忧（数据空白）
```

#### 形式 E：数据可视化（图表）

如果分析涉及定量数据或趋势对比，可以生成 Python 图表代码（matplotlib/seaborn）：

```python
import matplotlib.pyplot as plt
import matplotlib
matplotlib.rcParams['font.sans-serif'] = ['SimHei']  # 中文支持

labels = ['操作步骤', '加载速度', '信任感', '信息清晰度']
scores = [3.2, 2.8, 4.1, 3.5]
plt.bar(labels, scores, color=['#e74c3c' if s < 3.5 else '#2ecc71' for s in scores])
plt.title('支付流程各维度用户满意度')
plt.ylabel('满意度（1-5）')
plt.savefig('wiki/synthesis/assets/payment-satisfaction.png', dpi=150)
plt.show()
```

#### 形式 F：Obsidian Canvas（画布）

适合可视化关系网络的分析场景：用户旅程、竞品关系、因果链路、概念地图。

**Canvas JSON 示例**（保存为 `wiki/synthesis/支付流程关系图.canvas`）：

```json
{
  "nodes": [
    {"id": "1", "type": "file", "file": "wiki/concepts/支付流程痛点.md", "x": 0, "y": 0, "width": 250, "height": 120},
    {"id": "2", "type": "file", "file": "wiki/concepts/加载速度.md", "x": 300, "y": -100, "width": 250, "height": 120},
    {"id": "3", "type": "file", "file": "wiki/entities/用户A.md", "x": -300, "y": 0, "width": 200, "height": 100},
    {"id": "4", "type": "text", "text": "🔴 **核心矛盾**\n安全性 vs 便捷性", "x": 300, "y": 150, "width": 200, "height": 80}
  ],
  "edges": [
    {"id": "e1", "fromNode": "3", "toNode": "1", "label": "反馈"},
    {"id": "e2", "fromNode": "1", "toNode": "2", "label": "相关痛点"},
    {"id": "e3", "fromNode": "1", "toNode": "4", "label": "待解决"}
  ]
}
```

**使用场景**：
- 梳理多个概念之间的因果关系
- 展示用户旅程中的痛点分布
- 呈现竞品功能对比的关系网络
- 在 Obsidian 中打开 `.canvas` 文件即可交互式浏览

---

### 步骤 5.5：归档决策

在询问用户是否归档前，LLM 应先自行评估本次分析的归档价值：

**应该归档** ✅：
- 综合了 2+ 个来源的信息（跨源分析有独立价值）
- 发现了非显而易见的洞察或关联
- 回答了可能被再次提问的问题
- 发现了新的矛盾或修正了已有结论

**不必归档** ❌：
- 答案已完全包含在某个现有 synthesis/ 页面中
- 纯事实查询（"X 的数据是多少？"）
- 探索性提问且未产生有价值结论
- 只是对单一来源的简单摘要

**归档时**：LLM 直接建议归档并给出文件名，用户确认即可。
**不归档时**：LLM 说明原因（如"本次查询结果与现有 synthesis/支付分析.md 重叠度高，建议不重复归档"），用户仍可覆盖决定。

---

### 步骤 6：询问用户是否将洞察归档

完成回答后，询问用户：

```
这次分析是否值得保存到知识库？

如果是，我将把这份分析归档到 wiki/synthesis/，
方便下次查询时直接引用，避免重复分析工作。

归档到：wiki/synthesis/洞察-支付流程痛点.md
确认归档？（是/否，或输入不同的文件名）
```

---

### 步骤 7：归档洞察（如用户确认）

如果用户选择归档，在 `wiki/synthesis/` 创建新页面：

```markdown
---
title: 洞察-支付流程痛点
date: 2024-03-20
tags: [支付, 痛点, 用户研究, 竞品]
sources: [sources/访谈-2024年3月用户访谈, sources/访谈-2024年1月用户访谈, sources/竞品-2024年2月竞品分析]
related: [concepts/痛点-支付流程, concepts/痛点-加载速度, entities/用户-A, entities/用户-B]
query: 我们的用户在支付流程中最大的痛点是什么？
---

# 洞察-支付流程痛点

> **查询**：我们的用户在支付流程中最大的痛点是什么？
> **生成日期**：2024-03-20
> **来源页面**：3 个（sources: 3）

[完整分析内容...]
```

同时更新 `wiki/index.md`，在 synthesis/ 部分添加条目（**纯文本，禁止 `[[]]`**）：

```
- 洞察-支付流程痛点 – 基于3份资料的支付体验综合分析
```

---

### 步骤 8：在 log.md 追加记录（仅归档时）

如果执行了归档，在 `log.md` 末尾追加：

```markdown
## [YYYY-MM-DD] query | 支付流程用户痛点

- 查询问题：我们的用户在支付流程中最大的痛点是什么？
- 搜索方式：qmd query（混合搜索）
- 涉及页面：sources/用户访谈-2024-03、sources/用户访谈-2024-01、sources/竞品分析-2024-02
- 归档到：synthesis/洞察-支付流程痛点.md
```

**注**：如果用户选择不归档，不记录到 log.md（避免日志膨胀）。

> 💡 **探索的复利效应**：通过归档机制，用户的每一次探索性提问都会像导入新资料一样，在知识库中不断积累复利——对比分析、发现的关联、分析结论，这些有价值的洞察不会消失在聊天记录中，而是成为知识库永久的一部分。

---

## qmd 搜索最佳实践

### 搜索词建议

| 查询目标 | 推荐搜索词 |
|---------|-----------|
| 特定功能的用户反馈 | `"功能名 用户反馈"` 或 `"功能名 体验"` |
| 竞品对比信息 | `"竞品名"` 或 `qmd search "competitor"` |
| 某用户的完整画像 | `qmd get entities/用户-A.md` |
| 某概念的所有相关内容 | `qmd query "概念名"` 再 `qmd multi-get` |

### 搜索无结果时

如果 qmd 未返回相关结果：

1. 尝试换用不同关键词（中英文均试）
2. 降低搜索范围，搜索更宽泛的概念
3. 直接读取 `wiki/index.md`，手动定位相关页面
4. 告知用户："目前知识库中尚无关于 X 的记录，建议导入相关资料后再查询"

### HYDE 搜索（高级）

对于复杂的假设性问题，可以使用 Hyde 搜索：

```bash
qmd query "如果要改进支付流程，最关键的因素是什么" --type hyde
```

Hyde 搜索会先生成假设性答案，再用答案作为查询向量，适合探索性研究问题。
